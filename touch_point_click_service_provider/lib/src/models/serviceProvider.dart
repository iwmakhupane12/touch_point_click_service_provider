class ServiceProvider {
  String id,
      name,
      phone,
      altPhone,
      email,
      address,
      regDate,
      lastSeen,
      category,
      invoiceNumUnique;
  bool phoneVerified, emailVerified, blockedStatus, onlineStatus, docsVerified;
  double totalRating;
  int numRated;
}
